import * as S from '../../styles';

function CheckoutButton({ bookingId }) {
  return (
    <S.Button variation="primary" size="small">
      Check out
    </S.Button>
  );
}

export default CheckoutButton;
